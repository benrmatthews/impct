<div class="box wide">
  <div id="wrapper" class="stories list">
    <div id="inside">
      <div class="clear"></div>
      <div class="clear"></div>
        <h1>Tags</h1>
        <h2>A list of tags you can add to each story. The bigger the tag, the more it's been used.</h2>
        <h3>Want to suggest a new tag? <a href="mailto:ben@montfort.io">Contact us</a>.</h3> 
      <div class="clear"></div>
    </div>
  </div>

  <% max_size = @tags.map{|t| t.stories_count }.max %>
  <% @tags.each do |tag| %>
    <% mod = (max_size.to_f / ((tag.stories_count.to_f == 0) ? 1 : tag.stories_count.to_f )) %>
    <%= link_to tag.tag, tag_path(tag), :class => tag.css_class,
      :style => "text-decoration: none; vertical-align: middle; " <<
      "font-size: #{((52 / (mod + 1)) + 8).ceil}pt; line-height: 1.5em;" %>
  <% end %>
</div>
